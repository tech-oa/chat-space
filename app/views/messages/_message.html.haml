
.chat-main__message-list__index
  = message.user.name
  .chat-main__message-list__index__date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  -# .chat-main__message-lower
  .chat-main__message-list__text
    - if message.content.present?
      %p.chat-main__message-list__index__text
        = message.content
    = image_tag message.image.url, class: 'chat-main__message-list__index__text__image' if message.image.present?

    -# - if message.content.present?
-# .chat-main__message-list__index-second
    
    
      